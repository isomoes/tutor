#import "../common.typ": *

= 修回：从审稿意见到录用 (Revision and Response)

大多数论文不是被直接录用，而是「修回后录用」。
本章讲如何用 AI 把审稿意见变成可执行的修改，并写出得体的逐条回复。

== 本阶段研究基础

- 同行评审怎么运作：审稿人是谁、major/minor revision 意味着什么。
- 回复信的礼仪：感谢—回应—修改说明；不争对错，只讲证据。
- 原则：*每一条意见都要回应，每一处承诺的修改都要真的出现在正文里*。

== AI 辅助的修回流程

+ *解析*：让 agent 把审稿意见拆成逐条的、可执行的任务列表
  （按「必须改 / 可以argue / 需要补实验」分类）；
+ *起草*：对每条意见起草 point-by-point 回复，保持统一的礼貌语气；
+ *追踪*：让 agent 核对——回复信里承诺的每处修改，正文里是否真的改了；
+ *把关*：科学层面的答辩（要不要补实验、如何反驳误解）由人决定，
  AI 只负责表达和追踪。

== 模板与示例

- 回复信模板/示例：参见本仓库 `paper/response.tex`。

== 本章练习

- 给定一份样例审稿意见（或用第七章「AI 审稿人」的输出），
  为贯穿示例产出一份回复信骨架：
  逐条任务列表 + point-by-point 回复草稿 + 修改追踪表。
