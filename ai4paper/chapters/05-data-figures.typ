#import "../common.typ": *

= 数据与图表 (Data, Figures, and Tables)

实验产出数据之后，需要把数据变成论文里能用的图和表。
本章讲两件事：不写代码地处理数据，以及用「提示 → 代码 → 渲染 → 迭代」
的循环做出出版质量的图。

== 本阶段研究基础

- 一张好的论文图的标准：一眼能看出结论、坐标轴与单位完整、图注自成一体。
- 常见错误：截图代替矢量图、颜色无法区分、图里信息和正文对不上。

== 数据处理：不需要自己写代码

- Excel / CSV 数据清洗、汇总、透视，直接用自然语言让 agent 完成。
- 原则：让 agent 生成*可以重跑的脚本*，而不是一次性的手工操作——
  数据更新时能一键重来。

== 图表生成：提示工程 + 迭代改进

生成出版质量图表的循环：

+ *Generate*：用结构化的 prompt 让 AI 生成绘图代码（说清数据、图型、风格约束）；
+ *Evaluate*：渲染输出，对照论文图的标准检查；
+ *Find issues*：找出问题（字号、配色、遮挡、比例）；
+ *Update prompt*：把问题写回 prompt；
+ *Iterate*：重复直到达到 camera-ready 质量。

这个「生成—评估—改提示」的模式不只适用于画图，
在后面写作、修回章节还会反复出现。

== 本章练习

- 用贯穿示例的实验数据（或提供的样例数据），
  让 agent 从原始数据出发生成一张 camera-ready 图：
  交付绘图脚本 + 最终图，并保证数据更新后脚本能直接重跑。
